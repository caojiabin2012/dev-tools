# 设置

## 功能说明

管理应用配置：开机自启动、关闭时最小化到托盘、检查更新。

## 文件

- `src-tauri/src/settings.rs` - 后端设置逻辑 + 自动更新检查
- `src/lib/settings-api.ts` - 前端 API
- `src/components/settings.tsx` - 设置界面
- `src/components/about.tsx` - 关于界面

## 配置存储

配置文件位于系统数据目录：
- Windows: `%LOCALAPPDATA%/dev-tools/settings.json`
- macOS: `~/Library/Application Support/dev-tools/settings.json`
- Linux: `~/.local/share/dev-tools/settings.json`

```json
{
  "auto_start": false,
  "close_to_tray": true,
  "shortcuts": {
    "clipboard": "Ctrl+Shift+V"
  }
}
```

## 快捷键配置 (v0.3.0+)

`AppSettings.shortcuts` 存储工具全局快捷键，默认剪切板 `Ctrl+Shift+V`。

- 设置页可编辑并即时生效（`update_shortcuts` → `register_shortcuts_for_app`）
- 持久化在 `settings.json`

## 实现原理

### 数据结构

```rust
#[derive(Debug, Clone, Serialize, Deserialize)]
pub struct AppSettings {
    pub auto_start: bool,
    pub close_to_tray: bool,
    pub shortcuts: HashMap<String, String>,  // 工具 ID → 快捷键
}
```

### 加载与保存

```rust
impl AppSettings {
    pub fn load() -> Self {
        // 1. 读取 JSON 文件，解析失败则返回默认值
        let mut settings = std::fs::read_to_string(&path)
            .and_then(|s| serde_json::from_str(&s))
            .unwrap_or_default();

        // 2. 同步 auto_start 与 OS 实际状态
        //    防止用户在系统层面修改了自启动配置后不同步
        if let Ok(enabled) = query_autostart_enabled() {
            settings.auto_start = enabled;
        }

        settings
    }

    pub fn save(&self) -> Result<(), String> {
        let json = serde_json::to_string_pretty(self)?;
        std::fs::write(path, json)?;
        Ok(())
    }
}
```

### 开机自启

使用 `auto-launch` crate，跨平台实现：

```rust
fn build_autostart() -> Result<AutoLaunch, String> {
    let exe_path = std::env::current_exe()?;
    AutoLaunchBuilder::new()
        .set_app_name("Dev Tools")
        .set_app_path(exe_path.to_str()?)
        .build()
}

fn query_autostart_enabled() -> Result<bool, String> {
    let autolaunch = build_autostart()?;
    autolaunch.is_enabled()  // 查询 OS 实际状态
}

fn update_autostart(enabled: bool) -> Result<(), String> {
    let autolaunch = build_autostart()?;
    if enabled {
        autolaunch.enable()?;
    } else if autolaunch.is_enabled().unwrap_or(false) {
        autolaunch.disable()?;  // 仅在已启用时才禁用
    }
    Ok(())
}
```

- **Windows**: 写入注册表 `HKCU\Software\Microsoft\Windows\CurrentVersion\Run`
- **macOS**: 创建 `~/Library/LaunchAgents/` plist 文件
- **Linux**: 创建 `~/.config/autostart/` .desktop 文件

`get_settings` 命令每次调用都会同步 `auto_start` 与 OS 实际状态，确保 UI 显示一致。

### 关闭到托盘

在 `lib.rs` 的 `setup` 中注册窗口事件：

```rust
main_window.on_window_event(move |event| {
    if let WindowEvent::CloseRequested { api, .. } = event {
        let settings = settings_for_tray.lock().unwrap();
        if settings.close_to_tray {
            api.prevent_close();       // 阻止关闭
            hide_main_window(&window); // 隐藏 + 从任务栏移除
        }
    }
});
```

### 托盘菜单与交互

```rust
// 创建右键菜单
let show_item = MenuItem::with_id(app, "tray_show", "显示窗口", true, None::<&str>)?;
let quit_item = MenuItem::with_id(app, "quit", "退出", true, None::<&str>)?;
let tray_menu = Menu::with_items(app, &[&show_item, &quit_item])?;

// 菜单事件处理
app.on_menu_event(|app, event| {
    match event.id().as_ref() {
        "tray_show" => show_main_window(&window),
        "quit" => app.exit(0),
        _ => {}
    }
});

// 左键单击/双击切换窗口，右键显示菜单
tray.set_show_menu_on_left_click(false)?;
tray.on_tray_icon_event(move |_tray, event| {
    match event {
        TrayIconEvent::Click { button: MouseButton::Left, .. }
        | TrayIconEvent::DoubleClick { button: MouseButton::Left, .. }
            => toggle_main_window(&handle),
        _ => {}
    }
});
```

### 窗口显示/隐藏辅助函数

```rust
fn show_main_window(window: &WebviewWindow) {
    let _ = window.set_skip_taskbar(false);  // 恢复任务栏图标
    let _ = window.unminimize();
    let _ = window.show();
    let _ = window.set_focus();
}

fn hide_main_window(window: &WebviewWindow) {
    let _ = window.hide();
    let _ = window.set_skip_taskbar(true);   // 从任务栏移除
}

fn toggle_main_window(app: &AppHandle) {
    if let Some(window) = app.get_webview_window("main") {
        if window.is_visible().unwrap_or(false) {
            hide_main_window(&window);
        } else {
            show_main_window(&window);
        }
    }
}
```

### 检查更新与安装

通过 GitHub Releases API（仓库 `caojiabin2012/dev-tools`）：

```rust
const GITHUB_REPO: &str = "caojiabin2012/dev-tools";

pub async fn check_for_update() -> Result<Option<UpdateInfo>, String> {
    // GET /repos/{repo}/releases/latest
    // is_newer_version(latest, current) 语义化比较
    // pick_download_url: Windows 优先 -setup.exe
}

pub async fn download_and_install_update(app: AppHandle, download_url: String) -> Result<String, String> {
    // 1. 下载到 temp
    // 2. Windows Unblock-File
    // 3. 后台启动 NSIS /S 或 msiexec /passive
    // 4. app.exit(0) 释放文件锁
}
```

前端：`useUpdate` 启动时检查；设置/关于页「立即更新」调用 `downloadAndInstallUpdate`。

详见 **06.about.md**。

## API 命令

| 命令 | 参数 | 返回 | 说明 |
|------|------|------|------|
| `get_settings` | - | `AppSettings` | 获取当前设置 (同步 OS 自启动状态) |
| `save_settings` | `auto_start: bool, close_to_tray: bool` | `AppSettings` | 保存设置并应用 |
| `get_app_version` | - | `String` | 获取版本号 |
| `check_for_update` | - | `Option<UpdateInfo>` | 检查是否有新版本 |
| `download_and_install_update` | `download_url: String` | `String` | 下载并启动安装器（v0.4.1） |
| `update_shortcuts` | `shortcuts: HashMap` | `()` | 更新全局快捷键 |

## 二次开发

- **添加新设置项**: 在 `AppSettings` 结构体添加字段，在 `save_settings` 命令添加参数
- **添加设置页面 UI**: 在 `settings.tsx` 添加新的 `SettingItem`
- **持久化方式**: 可替换为 SQLite 或 tauri-plugin-store
- **集成自动更新**: 已实现应用内下载 + 静默安装，见 06.about.md
