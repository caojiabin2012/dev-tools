## v0.4.3 更新说明

### 修复
- **自动更新**：对齐 cc-switch，改用 `tauri-plugin-updater` 官方 `check → download → install` 流程
- **Windows 安装**：安装前移除托盘图标，避免安装器无法启动
- **发布产物**：启用 `createUpdaterArtifacts`，Windows 使用 MSI、macOS 使用 tar.gz 作为 updater 包
