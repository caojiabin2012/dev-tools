## v0.4.2 更新说明

### 修复
- **自动更新安装**：改用 Tauri NSIS 推荐的 `/UPDATE /P /R` 参数（被动进度 + 安装后重启）
- **进程隔离**：通过 PowerShell `Start-Process` 启动安装器，避免应用退出时连带终止安装进程
- **诊断日志**：更新过程写入 `%LOCALAPPDATA%\tool-kit\update.log`
